// Fixed set of department options for the "Add Employee" form.
// (Not fetched from the server — there's no /api/departments endpoint,
// this is just UI configuration.)
export const DEPARTMENTS = ['Engineering', 'Design', 'Sales', 'HR', 'Finance'];
